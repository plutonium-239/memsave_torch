name: Ruff
on: [push, pull_request]
jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ruff
        env:
          RUFF_OUTPUT_FORMAT: github
        run: |
          pip install ruff
          ruff format --check
          ruff check
